# Core LLM client libraries
openai>=1.0.0
anthropic>=0.20.0
litellm>=1.30.0

# tau-bench (install separately via: pip install -e . from the tau-bench repo)
# https://github.com/sierra-research/tau-bench

# vLLM inference server (install separately: pip install vllm)
# https://docs.vllm.ai/

# Standard library (included with Python, listed for reference)
# json, re, time, sys, typing
